using UnityEngine;
using System.Collections;

public class MapInfo : MonoBehaviour {

	public string mapID = "meadow01";
	public string mapName = "Meadow Map";
	//public Transform cameraTransform;


	// Use this for initialization
	void Start() {
		//cameraTransform.transform.position = new Vector3(56,30,-3); 
		//cameraTransform.transform.rotation = new Quaternion();
		//cameraTransform.transform.localScale = new Vector3(1,1,1); 
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
